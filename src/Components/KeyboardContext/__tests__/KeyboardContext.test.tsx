import { render, screen } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import KeyboardContext from "../KeyboardContext";

describe("Keyboard State", () => {
    it("when context is requested it returns an object", () => {

    });

    it("when writeCharacter is called it adds a single character to the end of the message", () => {
        
    });

    it("when modifyCharacter is called it changes the shift state to true", () => {

    });

    it("when deleteCharacter is called it removes the last character from the message", () => {
            
    });
});